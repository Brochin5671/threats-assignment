import { render } from '@testing-library/react'
import { ThreatTable } from '../components/ThreatTable'

describe('ThreatTable', () => {
  it('renders the ThreatTable component', () => {
    render(<ThreatTable />)
  })
})
